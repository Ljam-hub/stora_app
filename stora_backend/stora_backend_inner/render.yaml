# Render Blueprint — deploy from the repo in one click.
# https://render.com/docs/blueprint-spec

databases:
  - name: stora-db
    plan: free
    databaseName: stora
    user: stora

services:
  - type: web
    name: stora-backend
    runtime: python
    plan: free
    buildCommand: "cd stora_backend/stora_backend_inner && ./build.sh"
    startCommand: "cd stora_backend/stora_backend_inner && chmod +x start.sh && ./start.sh"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.12.5"
      - key: EMAIL_HOST
        value: "smtp.gmail.com"
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_USE_TLS
        value: "True"
      - key: EMAIL_HOST_USER
        sync: false
      - key: EMAIL_HOST_PASSWORD
        sync: false
      - key: DEFAULT_FROM_EMAIL
        sync: false
      # Set these securely in the Render dashboard:
      # - EMAIL_HOST_USER           (e.g., your verified Gmail / Brevo sender)
      # - EMAIL_HOST_PASSWORD       (e.g., your 16-digit Google App Password)
      # - DEFAULT_FROM_EMAIL        (e.g., STORA <your-email@gmail.com>)
      # - DATABASE_URL              (your Supabase connection string)
      # - CLOUDINARY_URL            (from your Cloudinary account)
      # - FIREBASE_CREDENTIALS_JSON (minified service account JSON for FCM push notifications)
      # - BREVO_API_KEY             (Free API key from brevo.com to send emails over HTTPS Port 443 — bypasses Render Free SMTP block)
      # - RESEND_API_KEY            (Alternative API key from resend.com)
      # - CORS_ALLOWED_ORIGINS      (if Flutter web is used)
